import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.upload.app',
  appName: 'upload-file-app-frontend',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
